# onedrive client for ubuntu

```sh
$ docker build -t xztaityozx/base ../../
$ docker build -t xztaityozx/onedrive-client-for-ubuntu ./
$ docker run --rm $PWD/result:/workdir xztaityozx/onedrive-client-for-ubuntu
```
